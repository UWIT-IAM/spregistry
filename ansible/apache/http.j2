#####-------------------------------------------
#
# apache port 80 configuration for spreg
#
#-------------------------------------------

RewriteCond %{SERVER_PORT} 80
RewriteCond %{HTTP_HOST} server-reg.cac.washington.edu
RewriteRule ^(.*)$ https://server-reg.cac.washington.edu/$1 [R,L]

RewriteCond %{SERVER_PORT} 80
RewriteCond %{REQUEST_URI} ^/sp-registry*
RewriteRule ^(.*)$ https://iam-tools.u.washington.edu/$1 [R,L]

