#--------------------------------------------
#
# apache port 80 configuration for spreg
#
#-------------------------------------------

RewriteCond %{SERVER_PORT} 80
RewriteCond %{REQUEST_URI} ^/spreg*
RewriteRule ^(.*)$ {{ spreg_spRegistryUrl }}$1 [R,L]

